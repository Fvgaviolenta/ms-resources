spring:
  datasource:
    url: ${MS_RESOURCES_DB_URL:jdbc:postgresql://localhost:5432/catastrofescl_resources}
    username: ${MS_RESOURCES_DB_USER:catastrofescl}
    password: ${MS_RESOURCES_DB_PASSWORD:catastrofescl}
    driver-class-name: org.postgresql.Driver
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASS:guest}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

catastrofescl:
  firebase:
    enabled: ${FIREBASE_ENABLED:false}
    project-id: ${FIREBASE_PROJECT_ID:}
    credentials-path: ${FIREBASE_CREDENTIALS_PATH:}
  auth:
    dev-mode: ${MS_RESOURCES_AUTH_DEV_MODE:true}
  cors:
    allowed-origins:
      - http://localhost:3000
      - http://localhost:3001

logging:
  level:
    cl.catastrofescl.resources: DEBUG
    org.hibernate.SQL: INFO
    org.springframework.amqp: INFO
